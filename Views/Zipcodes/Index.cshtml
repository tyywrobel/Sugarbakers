@model IEnumerable<Sugarbakers.Models.Zipcode>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Zipcode1">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Zipcode1">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Zipcode1">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
